# Agentic skills

Procedures for agents. Each folder is one skill (`SKILL.md` + YAML `name` / `description`).

See the [root README](../../README.md) for install, sources, and which skills map to investing / founding / creating.
